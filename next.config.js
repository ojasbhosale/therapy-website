/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'img.notionusercontent.com',
      'images.unsplash.com',
      'file.notion.so'
    ],
  },
};

module.exports = nextConfig;
